<UserControl x:Class="WpfCQRSDemo.Views.LeaderboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Leaderboard" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <ListView Grid.Row="1" ItemsSource="{Binding Entries}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Player" DisplayMemberBinding="{Binding PlayerName}" Width="200"/>
                    <GridViewColumn Header="Wins" DisplayMemberBinding="{Binding Wins}" Width="80"/>
                    <GridViewColumn Header="Losses" DisplayMemberBinding="{Binding Losses}" Width="80"/>
                    <GridViewColumn Header="Draws" DisplayMemberBinding="{Binding Draws}" Width="80"/>
                    <GridViewColumn Header="Points" DisplayMemberBinding="{Binding Points}" Width="80"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
